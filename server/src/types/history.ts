export interface visits {
    id: string;
    sender_id: string;
    sender_name: string;
    created_at: string;
}

export interface likes {
    id: string;
    sender_id: string;
    sender_name: string;
    created_at: string;
}