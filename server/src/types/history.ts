export interface visits {
    id: string;
    sender_id: string;
    sender_name: string;
    createdAt: string;
}

export interface likes {
    id: string;
    sender_id: string;
    sender_name: string;
    createdAt: string;
}