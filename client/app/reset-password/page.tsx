import ResetPassword from "@/components/pages/ResetPassword";
import React, { Suspense } from "react";

const page = () => {
  return (
    <Suspense>
      <ResetPassword />
    </Suspense>
  );
};

export default page;
