import ConfirmEmail from "@/components/pages/ConfirmEmail";
import React, { Suspense } from "react";

const page = () => {
  return (
    <Suspense>
      <ConfirmEmail />
    </Suspense>
  );
};

export default page;
